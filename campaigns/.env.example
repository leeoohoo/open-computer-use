# Supabase (same as backend)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE=your-service-role-key

# WorkMail SMTP
CAMPAIGN_FROM_EMAIL=
CAMPAIGN_FROM_PASSWORD=
CAMPAIGN_FROM_NAME=
WORKMAIL_SMTP_HOST=
WORKMAIL_SMTP_PORT=

# Tracking (public URL where backend is reachable)
BACKEND_PUBLIC_URL=

# Rate limiting
SEND_RATE_PER_MINUTE=25
